#include <stdio.h>
/**
* main - check the code for Holberton School students.
*
* Return: Always 0.
*/
int main(void)
{
int divisor = 2;
long int numero = 612852475143;
long factor = numero;

while (factor > 1)
{
	if ((factor % a) == 0)
	{
		factor /= divisor;
	}
	else
	{
		divisor++;
	}

}
	printf("%d\n", divisor);
	return (0);
}
